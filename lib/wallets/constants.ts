export {
  DEFAULT_CURRENCY,
  EARNINGS_CLEARANCE_DAYS,
  MIN_WITHDRAWAL_KOBO,
  PLATFORM_FEE_BPS,
} from "@/config/constants";

/** Estimated Paystack processing fee (basis points). */
export const PAYMENT_FEE_BPS = 150;

/** Flat withdrawal processing fee (kobo). */
export const PAYOUT_FEE_KOBO = 0;
